import themes from "themes";

export default {
    titleContainer : {
        ...themes.centerItems,
        ...themes.pv80
    },

    text : {
        ...themes.text,
        ...themes.textBlack,
        ...themes.textCenter
    }
};